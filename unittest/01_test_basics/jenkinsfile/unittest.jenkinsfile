// this pipeline only serves unittest test cases
pipeline {
    agent any

    stages {
        stage('Unittest') {
            steps {
                sh 'cd unittest/01_test_basics/'
                sh 'chmod +x ./run_tests.sh'
                sh './run_tests.sh'
            }
        }
    }
}