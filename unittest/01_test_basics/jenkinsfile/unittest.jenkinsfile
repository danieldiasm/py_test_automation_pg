// this pipeline only serves unittest test cases
pipeline {
    agent any

    stages {
        stage('Unittest') {
            steps {
                sh 'python -m unittest discover ./application/'
            }
        }
    }
}