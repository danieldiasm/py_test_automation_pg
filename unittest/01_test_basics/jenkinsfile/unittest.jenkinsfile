// this pipeline only serves unittest test cases
pipeline {
    agent any

    stages {
        stage('Unittest') {
            steps {
                sh './run_tests.sh'
            }
        }
    }
}